CreateSavage

 

Select song:

Shawn Mendes - If I Can't Have You

Run 

BlocksShow CodeStart OverWorkspace

English

©

Even top universities teach block-based coding (e.g., Berkeley, Harvard). But under the hood, the blocks you have assembled can also be shown in JavaScript, the world's most widely used coding language:

var Cledis;

var Anglelica;

whenSetup(function () {

  makeNewDanceSpriteGroup(12, "DUCK", "circle");

  makeNewDanceSprite("CAT", 'Cledis', {x: 200, y: 100});

  makeAnonymousDanceSprite("UNICORN", {x: 100, y: 300});

  changeMoveLR('Anglelica', MOVES.ClapHigh, -1);

  setBackground('#6600cc');

  changeMoveEachLR("DOG", MOVES.Dab, 1);

  makeAnonymousDanceSprite("CAT", {x: 300, y: 300});

});

OK

